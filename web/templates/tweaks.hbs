{{#if loading}}
			<div class="text-center"><img src="img/loading.gif" class="img-fluid"/></div>
{{else}}
	<div class="jumbotron">
	  <p class="lead">The following tweaks are made to captions as they are processed, add or edit tweaks below to fix common transcription or case issues. The required format is one peroperty per line, for example: A.W.S.=AWS note that the case of the left hand expression is ignored but the case of the right hand expression is preserved.</p>
	</div>		
	<div class="px-2">
		<form>
			<div class="form-group">
				<button type="button" class="btn btn-primary mb-2" onclick="javascript:saveTweaks($('#tweaks').val()); return false;">Save tweaks</button>
			</div>
			<div class="form-group">
				<textarea name="tweaks" id="tweaks" class="form-control" rows="10" placeholder="One entry per line a=b">{{#each tweaks}}
{{this}}{{/each}}</textarea>
			</div>
			<div class="form-group">
				<button type="button" class="btn btn-primary mb-2" onclick="javascript:saveTweaks($('#tweaks').val()); return false;">Save tweaks</button>
			</div>
		</form>
	</div>
{{/if}}